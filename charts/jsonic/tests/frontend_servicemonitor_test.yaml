suite: Frontend Service Monitor
templates:
  - frontend/deployment.yaml
  - frontend/servicemonitor.yaml
  - configmap.yaml
  - secrets.yaml
values:
  - ../values.yaml
tests:
  - it: should generate ServiceMonitor when deployment mode is distributed and metrics are enabled
    template: frontend/servicemonitor.yaml
    set:
      deploymentMode: distributed
      frontend:
        metrics:
          enabled: true
          serviceMonitor:
            enabled: true
    asserts:
      - containsDocument:
          apiVersion: monitoring.coreos.com/v1
          kind: ServiceMonitor
      - equal:
          path: metadata.name
          value: release-name-jsonic-frontend

  - it: should not generate ServiceMonitor when deployment mode is distributed and metrics are disabled
    template: frontend/servicemonitor.yaml
    set:
      deploymentMode: distributed
      frontend:
        metrics:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not generate ServiceMonitor when deployment mode is not distributed
    template: frontend/servicemonitor.yaml
    set:
      deploymentMode: distributed
      frontend:
        metrics:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0
