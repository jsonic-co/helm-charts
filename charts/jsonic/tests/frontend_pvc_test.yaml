suite: Frontend PVC
templates:
  - frontend/deployment.yaml
  - frontend/pvc.yaml
  - configmap.yaml
  - secrets.yaml
values:
  - ../values.yaml
tests:
  - it: should generate PVC when deployment mode is distributed and persistence is enabled
    template: frontend/pvc.yaml
    set:
      deploymentMode: distributed
      frontend:
        persistence:
          enabled: true
    asserts:
      - containsDocument:
          apiVersion: v1
          kind: PersistentVolumeClaim
      - equal:
          path: metadata.name
          value: release-name-jsonic-frontend

  - it: should not generate PVC when deployment mode is distributed and persistence is disabled
    template: frontend/pvc.yaml
    set:
      deploymentMode: distributed
      frontend:
        persistence:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not generate PVC when deployment mode is distributed and existing claim is set
    template: frontend/pvc.yaml
    set:
      deploymentMode: distributed
      frontend:
        persistence:
          enabled: true
          existingClaim: my-existing-claim
    asserts:
      - hasDocuments:
          count: 0

  - it: should not generate PVC when deployment mode is not distributed
    template: frontend/pvc.yaml
    set:
      deploymentMode: aio
      frontend:
        persistence:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0
