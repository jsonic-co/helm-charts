suite: Admin Ingress
templates:
  - admin/deployment.yaml
  - admin/ingress.yaml
  - admin/service.yaml
  - configmap.yaml
  - secrets.yaml
values:
  - ../values.yaml
tests:
  - it: should generate ingress when deployment mode is distributed
    template: admin/ingress.yaml
    set:
      deploymentMode: distributed
      admin:
        ingress:
          enabled: true
    asserts:
      - containsDocument:
          apiVersion: networking.k8s.io/v1
          kind: Ingress
      - equal:
          path: metadata.name
          value: release-name-jsonic-admin

  - it: should not generate ingress when deployment mode is not distributed
    template: admin/ingress.yaml
    set:
      deploymentMode: aio
    asserts:
      - hasDocuments:
          count: 0
