suite: Frontend Network Policy
templates:
  - frontend/networkpolicy.yaml
  - configmap.yaml
  - deployment.yaml
  - secrets.yaml
  - service.yaml
values:
  - ../values.yaml
tests:
  - it: should generate network policy when deployment mode is distributed and network policy is enabled
    template: frontend/networkpolicy.yaml
    set:
      deploymentMode: distributed
      frontend:
        networkPolicy:
          enabled: true
    asserts:
      - containsDocument:
          apiVersion: networking.k8s.io/v1
          kind: NetworkPolicy
      - hasDocuments:
          count: 1

  - it: should not generate network policy when deployment mode is distributed and network policy is disabled
    template: frontend/networkpolicy.yaml
    set:
      deploymentMode: distributed
      frontend:
        networkPolicy:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not generate network policy when deployment mode is not distributed
    template: frontend/networkpolicy.yaml
    set:
      deploymentMode: aio
      frontend:
        networkPolicy:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0
