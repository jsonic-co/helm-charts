{{- if and (eq .Values.deploymentMode "distributed") .Values.frontend.persistence.enabled (not .Values.frontend.persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jsonic.fullname" . }}-frontend
  namespace: {{ include "jsonic.namespace" . | quote }}
  labels:
    app.kubernetes.io/component: jsonic-frontend
    {{- include "jsonic.labels" . | nindent 4 }}
  {{- if or .Values.commonAnnotations .Values.frontend.persistence.annotations }}
  annotations:
    {{- if .Values.commonAnnotations }}
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
    {{- end }}
    {{- if .Values.frontend.persistence.annotations }}
    {{- toYaml .Values.frontend.persistence.annotations | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  accessModes:
    {{- range .Values.frontend.persistence.accessModes }}
    - {{ . | quote }}
    {{- end }}
  resources:
    requests:
      storage: {{ .Values.frontend.persistence.size | quote }}
  {{- if .Values.frontend.persistence.storageClass }}
  {{- if (eq "-" .Values.frontend.persistence.storageClass) }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.frontend.persistence.storageClass | quote }}
  {{- end }}
  {{- else if .Values.global.defaultStorageClass }}
  storageClassName: {{ .Values.global.defaultStorageClass | quote }}
  {{- end }}
  {{- if .Values.frontend.persistence.selector }}
  selector:
    {{- toYaml .Values.frontend.persistence.selector | nindent 4 }}
  {{- end }}
  {{- if .Values.frontend.persistence.dataSource }}
  dataSource:
    {{- toYaml .Values.frontend.persistence.dataSource | nindent 4 }}
  {{- end }}
{{- end }}
