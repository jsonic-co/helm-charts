{{- if and (eq .Values.deploymentMode "distributed") .Values.backend.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "jsonic.fullname" . }}-backend
  namespace: {{ include "jsonic.namespace" . | quote }}
  labels:
    app.kubernetes.io/component: jsonic-backend
    {{- include "jsonic.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
  {{- end }}
spec:
  policyTypes:
    - Ingress
    - Egress
  podSelector:
    matchLabels:
      app.kubernetes.io/component: jsonic-backend
      {{- include "jsonic.selectorLabels" . | nindent 6 }}
  egress:
    {{- if .Values.backend.networkPolicy.allowExternalEgress }}
    - {}
    {{- else }}
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- if .Values.backend.networkPolicy.extraEgress }}
    {{- toYaml .Values.backend.networkPolicy.extraEgress | nindent 4 }}
    {{- end }}
    {{- end }}
  ingress:
    - ports:
        - port: {{ .Values.backend.containerPorts.http }}
        - port: {{ .Values.backend.containerPorts.https }}
      {{- if not .Values.backend.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{- include "jsonic.selectorLabels" . | nindent 14 }}
        {{- if .Values.backend.networkPolicy.addExternalClientAccess }}
        - podSelector:
            matchLabels:
              {{ include "jsonic.fullname" . }}-client: "true"
        {{- end }}
        {{- if .Values.backend.networkPolicy.ingressPodMatchLabels }}
        - podSelector:
            matchLabels:
              {{- .Values.backend.networkPolicy.ingressPodMatchLabels | nindent 14 }}
        {{- end }}
        {{- if .Values.backend.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- .Values.backend.networkPolicy.ingressNSMatchLabels | nindent 14 }}
          {{- if .Values.backend.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- .Values.backend.networkPolicy.ingressNSPodMatchLabels | nindent 14 }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.backend.networkPolicy.extraIngress }}
    {{- toYaml .Values.backend.networkPolicy.extraIngress | nindent 4 }}
    {{- end }}
{{- end }}
